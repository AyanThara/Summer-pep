//vector
#include<iostream>
#include<vector>
using namespace std;
int main(){
    vector<int> v;
    v.push_back(10);
    v.push_back(20);
    v.push_back(30);// to add an element at the last 
    v.pop_back();//to remove an element 
    v.front();// to check the first elemeent 
    v.back();//to get the element at the last 
    v.size();// to calculate the size
}

some more vector functions 
v.empty();     // true if empty
v.clear();     // remove all elements
v.at(0);       // check the element at the 0 th index 

*/


